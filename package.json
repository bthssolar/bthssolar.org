{
  "name": "bths-solar-car.github.io",
  "version": "0.0.2",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && mkdir dist && rsync -rv * dist --exclude ./dist",
    "deploy": "rsync -rv --delete * $1 --exclude ./dist"
  },
  "dependencies": {
    "three": "^0.125.2"
  },
  "devDependencies": {}
}
